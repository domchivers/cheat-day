/* Accounts + sync are optional. To turn them on, paste your Firebase web-app config here
 * (Firebase console → Project settings → Your apps → "SDK setup and configuration" → Config).
 * It looks like:
 *   window.FIREBASE_CONFIG = { apiKey: "AIza…", authDomain: "cheat-days.firebaseapp.com", projectId: "cheat-days",
 *                              storageBucket: "…", messagingSenderId: "…", appId: "…" };
 * This config is not a secret; access is controlled by the Firestore rules in the README.
 * Leave it null and the app simply works on one device at a time. */
window.FIREBASE_CONFIG = null;
